.log_box
  - if current_user == nil
    != render partial: 'login_form'
  - else
    %P<
      Logged in as
      %span{ class: "user_id_grab", data: { id: current_user.id }} #{current_user.name}
      %span> .
    %P
      = link_to 'View your profile', user_path(current_user)
    %P
      = link_to 'Log out', session_path(session[:session_id]), method: :delete